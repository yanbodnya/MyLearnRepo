A) есть текстовый файл, содержащий слова(без переносов) и знаки
препинания (например, текст рассказа). нужно вывести список слов в
алфавитном порядке, с указанием количества их вхождения в текст.

B) есть текстовый файл, содержащий список url'ов. нужно закачать
страницы по этим url.

C) есть произвольный текстовый файл (например html), в котором
присутствуют email'ы, причём email'ы не обязаны быть уникальны. нужно
вывести
 1) список email'ов
 2) список доменов с указанием сколько уникальных email'ов из данного
домена найдено.